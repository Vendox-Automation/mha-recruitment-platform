# Production / runtime dependencies (pinned). Canonical DB is PostgreSQL.
# See docs/architecture/ADR-0001-stack-and-structure.md.
Django==5.2.15
djangorestframework==3.17.1
django-cors-headers==4.9.0
dj-database-url==3.1.2
psycopg[binary]==3.3.4
python-dotenv==1.2.2
# Pure-Python PDF text extraction for Smart Job Fit resume keyword extraction
# (DOCX uses the stdlib zipfile; no extra dependency). ADR-0001 §9.3.
pypdf==5.1.0
