/** TanStack Query keys for the candidate feature (single source of truth). */
export const CANDIDATE_PROFILE_KEY = ["candidate", "profile"] as const;
export const CANDIDATE_DASHBOARD_KEY = ["candidate", "dashboard"] as const;
